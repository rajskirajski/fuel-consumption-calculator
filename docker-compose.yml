services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "9000:8080"
    environment:
      APP_NAME: "Fuel Consumption Calculator"
      VERSION: "1.0.0"
      ENVIRONMENT: "local"
